# 📚 ETRI Human Understanding AI Study Log (2026.05.15)

본 레포지토리(`dacon-etri-human-understanding`)에서 진행된 멀티모달 라이프로그 데이터 분석 및 전처리 파이프라인 구축 과정을 정리한 기록입니다.

---

## 🧬 Part 1: 멀티모달 데이터 전처리 파이프라인 구축 (`src/`)

이질적인 센서 데이터를 통합하고, 모델 학습에 적합한 피처로 변환하는 전처리 엔진(`MultiModalPreprocessor`)을 설계했습니다.

### 1. 다해상도 피처 엔지니어링 (Feature Engineering)
*   **시계열 통계 추출**: 초/분 단위 센서 데이터를 일일 단위로 집계하며 평균뿐만 아니라 분산, 최대값, 엔트로피 등을 추출하여 데이터의 동역학(Dynamics)을 보존함.
*   **활동 엔트로피(Activity Entropy)**: 사용자의 행동 유형 분포를 통해 일상의 단조로움이나 변화량을 수치화함.
*   **일주기 리듬 가중치(Circadian Weighting)**: 조도(Light) 데이터 전처리 시, 수면 질에 큰 영향을 미치는 야간 시간대(20시~04시) 데이터에 가중치를 부여하는 로직 구현.

### 2. 데이터 정규화 및 정제
*   **단위 정규화 (Unit Normalization)**: GPS 속도 데이터 등에서 발생하는 단위 불일치(km/h vs m/s)를 감지하고 표준 단위로 통합하는 정제 로직 적용.
*   **중첩 데이터 파싱**: List나 Dict 형태로 저장된 복잡한 센서 신호(GPS, Heart Rate)를 플래닝하여 분석 가능한 형태로 변환.

### 3. 데이터 강건성(Robustness) 확보
*   **Null-mask Embedding**: 결측치를 단순히 0으로 채우지 않고, 데이터의 존재 여부를 나타내는 바이너리 인디케이터(`is_missing_...`)를 생성하여 모델이 데이터의 신뢰도를 판단할 수 있게 설계.

---

## 📖 Part 2: 연구 가이드 및 방법론 분석 (`docs/`)

경진대회 승리를 넘어 학술적 성과(ICTC 2026) 도출을 위한 기술 전략을 수립했습니다.

*   **데이터 아키텍처 분석**: 스마트폰, 워치, 수면 매트 등 기기별 특성과 샘플링 주기(1Hz ~ 1/600Hz)의 차이를 분석하여 Feature Alignment 전략 수립.
*   **타겟 레이블 분석**: 수면 질, 피로도, 스트레스 등 7가지 지표의 레이블링 로직(1: 긍정, 0: 부정)을 파악하여 모델의 해석력(Interpretability) 확보 방안 검토.
*   **평가 메커니즘**: Log-Loss 기반의 평가 산식과 Public/Private 데이터 구성에 따른 교차 검증(Cross-Validation) 전략 수립.

---

## 🛠️ Part 3: 프로젝트 구조 자동화 및 환경 설정

*   **모듈화된 구조**: `src/`, `data/`, `docs/`, `notebooks/`로 구분된 표준 프로젝트 레이아웃을 통해 연구의 재현성과 확장성 확보.
*   **환경 격차 해소**: `.vscode/settings.json` 및 `requirements.txt`를 통해 팀 내 개발 환경(Python 경로, 라이브러리 의존성)을 통일.

---

## 🎯 향후 실행 계획

1.  **데이터 정렬 고도화**: 기기 간의 미세한 타임스탬프 오차를 교정하는 `Temporal Alignment` 로직 추가.
2.  **피처 중요도 분석**: 전처리된 피처들이 7대 타겟 지표에 미치는 상관관계를 분석하여 차원 축소 및 최적 피처 선정.
3.  **학술 논문 초안 작성**: 구현된 전처리 기법의 독창성(Circadian Weighting 등)을 바탕으로 방법론 섹션 기술.

---
*마지막 업데이트: 2026-05-15*
